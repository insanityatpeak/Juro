import { CASES } from "@/data/cases";

// Backwards-compatible alias. The three real cases live in cases.ts; this is the
// first one (the MRI/ERISA case), used as the default sample for the landing demo.
export const sampleTranscript = CASES[0].transcript;
